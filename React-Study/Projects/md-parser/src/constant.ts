import { ReadonlyDeep } from "type-fest";

const display_style = {
  EDITOR_HEIGHT: '80vh',
  EDITOR_WIDTH: '48vw',
}

/** style 관련 상수값 */
export const DISPLAY_STYLE: ReadonlyDeep<typeof display_style> = display_style;